[project]
name = "gcp-platform-docs"
version = "0.1.0"
description = "mkdocs environment for gcp-platform's documentation site"
requires-python = ">=3.10"
dependencies = [
  "mkdocs>=1.6",
  "mkdocs-material>=9.5",
]
